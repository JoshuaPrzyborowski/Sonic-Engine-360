//==============================================================================
    strength        = 11;     // Strength of the spring
    bounce_factor   = 0.55;   // Factor of player speed to add to momentum 
    max_speed       = 14.5;   // Max speed allowed from the spring
    angle           = 45;     // Angle in degrees it's facing
    offset          = 0;      // Offset from the sprite origin to use for the collision line (the lower the value the larger the action area will be)

action_inherited();
